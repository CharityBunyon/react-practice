import React from 'react';

import './Auth_page.scss';

const first = [1, 2, 3];
const second = [4, 5, 6];

const combine = [...first, ...second];
console.error(combine);

class Auth extends React.Component {
  render() {
    return (
        <div>Hello</div>

    );
  }
}

export default Auth;
